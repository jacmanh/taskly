# Database
DATABASE_URL="postgresql://taskly:taskly_dev_password@localhost:5566/taskly_dev?schema=public"

# API
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:4200

# JWT Authentication
# Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_64_characters_recommended
JWT_REFRESH_SECRET=your_refresh_secret_here_different_from_jwt_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Security
BCRYPT_ROUNDS=12
COOKIE_SECRET=your_cookie_secret_here
